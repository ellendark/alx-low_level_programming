nlcude < stdio.h >
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int i = '0'; i <= '9'; i++)
				{
		putchar(i);
	}
	for (int a = 'A'; a <= 'F'; a++)
	{

		putchar(a);
	}

	putchar('\n');
	return (0);
}					
